// ⚖️ Design Decisions (1–20)
// JWT vs Session — কোনটা নেবে এবং exact reason কী?
// Monolith vs Microservice — ছোট project-এ কোনটা better?
// SQL vs NoSQL — user system design করলে কোনটা use করবে?
// Redis cache ব্যবহার করবে কিনা — decision কীভাবে নেবে?
// API Gateway দরকার কিনা — কখন প্রয়োজন?
// GraphQL vs REST — কোনটা choose করবে?
// Background job system লাগবে কিনা — কখন দরকার?
// WebSocket vs Polling — real-time feature-এ কোনটা?
// CDN use করবে কিনা — backend decision কেন?
// File storage local vs cloud — কোনটা better?
// Logging level কতটুকু রাখবে production-এ?
// Single DB vs multiple DB — scaling decision কী?
// Hard delete vs soft delete — কোনটা use করবে?
// UUID vs auto-increment ID — কোনটা better?
// Centralized config vs distributed config — decision কী?
// Synchronous vs asynchronous processing — কখন কোনটা?
// One big service vs multiple small services — decision কী?
// ORM vs raw query — performance vs maintainability?
// Rate limiting strict vs flexible — কোনটা better?
// Authentication middleware global vs route-specific — decision?