// 🌐 Networking & Protocols (16–30)
// HTTP keep-alive কী? performance-এ কীভাবে help করে?
// TLS termination কোথায় করা উচিত (load balancer vs app)?
// HTTP/1.1 vs HTTP/2 — backend impact কী?
// কীভাবে handle করবে slow client problem?
// Nagle’s algorithm কী? latency-তে impact কী?
// কীভাবে detect করবে network partition?
// কীভাবে design করবে retry-safe API (network failure)?
// কীভাবে prevent করবে duplicate request due to retry?
// Circuit breaker trip condition কীভাবে decide করবে?
// কীভাবে design করবে timeout strategy (client vs server)?
// কীভাবে handle করবে DNS failure scenario?
// Reverse proxy (NGINX) backend-এ কী role play করে?
// কীভাবে prevent করবে request smuggling attack?
// কীভাবে handle করবে long-lived connections?
// কীভাবে optimize করবে API latency across regions?