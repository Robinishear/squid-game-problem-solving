// 🧠 Debugging & “Why this broke?” (1–15)
// API হঠাৎ 500 error দিচ্ছে কিন্তু code change হয়নি — কীভাবে debug করবে?
// Production-এ শুধু কিছু user error পাচ্ছে, সবাই না — possible কারণ কী?
// Local-এ ঠিক কাজ করছে কিন্তু server-এ fail করছে — approach কী?
// DB query randomly slow — কখনো fast, কখনো slow — কেন?
// Memory usage slowly বাড়ছে — crash না হলেও — issue কী হতে পারে?
// Server restart দিলে problem solve হয় — root cause কী হতে পারে?
// Same API মাঝে মাঝে duplicate data insert করছে — কেন?
// Log-এ error নাই কিন্তু user complain করছে — কী করবে?
// CPU usage 100% but traffic normal — কেন?
// Suddenly DB connections exhausted — কীভাবে fix করবে?
// API response delay only at night — possible কারণ কী?
// Cache clear করলে bug fix হয় — root issue কোথায়?
// Only mobile users bug পাচ্ছে — backend issue হতে পারে?
// Third-party API slow হলে system hang করছে — fix কী?
// Random timeout error — debugging strategy কী?